const rows=[["Website / landing page","BUILD / VERIFY","Primary commercial funnel"],["£199 checkout","APPROVAL GATE","Keep test/live isolated"],["Shopify stores","MONITOR","4 stores registered"],["Domains","MONITOR","4 sale assets + pricing register"],["PostHog","CONNECT","Analytics and conversion evidence"],["Notion","CONNECT","Projects / tasks / decisions"],["Agent worker","ACTIVATE","Durable worker + queue required"],["Heartbeat","ACTIVE","GitHub scheduled recovery"]]; export default function Dashboard(){return <main><div className="wrap"><nav className="nav"><a className="brand" href="/">← SCOTTISH PRIDE</a><div className="pill">CONTROLLED AUTONOMY</div></nav><section className="hero"><div className="eyebrow">Operations</div><h1>Business command centre</h1><p>Single view of the operating system. Status is based on configured state; production claims require live evidence.</p></section><div className="grid">{rows.map(([a,b,c])=><div className="card" key={a}><h3>{a}</h3><strong>{b}</strong><p className="muted">{c}</p></div>)}</div><section className="section"><h2>Agent rules</h2><p className="muted">RUN allowed for reversible, bounded actions. ESCALATE for payments, refunds, contracts, legal commitments, public claims, destructive production changes, secrets and new paid advertising. SAFE mode on missing evidence or credentials.</p></section></div></main>}